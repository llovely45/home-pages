# VitePress 个人前端导航

<p align="center"> 基于 <b>VitePress</b> 的个人前端导航页面模板 </p>
<p align='center'>原生模板作者：<a href="https://notes.fe-mm.com/">茂茂 | maomao</a></p>

> 此项目基于原模板进行了深度定制，支持 Markdown 数据驱动和 NPM 生态。

---

## ✨ 新特性

- **Markdown 数据驱动**: 直接修改根目录下的 `data.md` 即可更新导航数据，无需触碰代码。
- **自动化生成**: 每次运行 `dev` 或 `build` 时，脚本会自动将 `data.md` 转换为 TypeScript 数据。
- **FontAwesome 6 支持**: 内置 FontAwesome 图标支持，直接使用图标名称（如 `github`, `envelope`）。
- **NPM 管理**: 完全迁移至 npm，移除 pnpm 依赖。

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 本地开发

```bash
npm run dev
```
访问 `http://localhost:8732` 查看效果。

### 3. 构建部署

```bash
npm run build
```
构建产物位于 `docs/.vitepress/dist`。

### 4. 部署到 Cloudflare Pages

本项目支持部署为**纯静态网站**到 Cloudflare Pages，不消耗 Workers 额度。

1. **登录 Cloudflare Dashboard**，进入 **Pages**。
2. **连接 Git 仓库**，选择本项目。
3. **构建设置**:
   - **框架预设 (Framework preset)**: 选择 `None` (或者手动配置)
   - **构建命令 (Build command)**: `npm run build`
   - **构建输出目录 (Build output directory)**: `dist`
4. **保存并部署**。

> [!TIP]
> 这是一个纯静态站点，Cloudflare Pages 会自动作为静态资源托管，不会调用 Functions，因此不消耗 Workers 调用额度。

## 📝 如何更新导航数据

1. 打开项目根目录下的 `data.md` 文件。
2. 按照以下格式添加或修改内容：

```markdown
# 板块标题 (例如：常用推荐)

## 网站标题 (例如：GitHub)
link: https://github.com
icon: github
desc: (可选) 网站描述，默认与标题相同
```

3. 保存文件。开发服务会自动热更新。

## 🎨 图标说明

项目集成了 FontAwesome Free Solid 图标库。
在 `data.md` 的 `icon:` 字段中，你可以直接填入图标名称（去掉了 `fa-` 前缀），例如：
- `GitHub` -> `code-branch` (或 `github` 如果是品牌图标)
- `邮箱` -> `envelope`
- `服务器` -> `server`

## 📄 License

MIT
